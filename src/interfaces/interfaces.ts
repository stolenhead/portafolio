export interface proyect {
  alias: string,
  size_expand:number,
  title?:string ,
  powers?: string[],
  body?:string,
  externa_link?:string,
  image_title?:string,
  image_url?: string ,
  color:number,
  back_color?:string 
}